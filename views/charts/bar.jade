extends ../chart

block chart
  script.    
    $(document).ready( function() {
      var height = window.innerHeight - $('.navbar').height() - 40;          
      $('#chart').height(height);                      
      var chart = barChart(d3.select('#chart'));   
      chart();      
      
      $(window).on('resize', function() {
        height = window.innerHeight - $('.navbar').height() - 40;         
        $('#chart').height(height);      
      });
    });
