extends ../layout

block content  
  #chart.wrapper()
  script
    include attributeChart.js
  script.
    $(document).ready( function() {
      var height = $('.sidebar').height();
      $('#chart').height(height);      
      attributeChart(d3.select('#chart'));
    });
    //- $(window).on('resize', function() {
    //-   var height = $('.sidebar').height();
    //-   $('#chart').height(height);
    //-   d3.select('#chart svg').remove();
    //-   attributeChart(d3.select('#chart'));
    //- });
    //- var chart = attributeChart(d3.select('#chart'));
    //- chart();
